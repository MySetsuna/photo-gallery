import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jxk.ionicdemo',
  appName: 'photo-gallery',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
